name: .NET

on:
  push:
    branches:
    - main
    - divim-patch-1
  pull_request:
    branches: 
      - main
      - divim-patch-1
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore –-project AzureAppDevWorkshops/Azure App Service/Tailspin.SpaceGame.Web/Tailspin.SpaceGame.Web.csproj
    - name: Publish
      run: dotnet publish -o AzureAppDevWorkshops/Azure App Service/Tailspin.SpaceGame.Web/publish

